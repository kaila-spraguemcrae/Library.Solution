@{
  Layout="_Layout";
}

@model Library.Models.Book

<h4>Search the bool by title</h4>

@using (Html.BeginForm())
{
    @Html.LabelFor(model=>model.Title)
    @Html.TextBoxFor(model=>model.Title)

    <input type="submit" value="Search By Title" />
}